{
  "name": "dockfm-retail",
  "version": "1.0.0",
  "description": "La piattaforma instore più moderna, automatizzata e centralizzata del mercato",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "player:dev": "npm run dev --workspace=@dockfm/player",
    "dashboard:dev": "npm run dev --workspace=@dockfm/dashboard",
    "api:dev": "npm run dev --workspace=@dockfm/api",
    "db:migrate": "npm run migrate --workspace=@dockfm/api",
    "db:seed": "npm run seed --workspace=@dockfm/api"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "author": "DockFm",
  "license": "UNLICENSED"
}
